{
  "name": "dcathedip",
  "version": "1.0.0",
  "description": "A static educational website for DCATheDip.com / DCATheDip.ca.",
  "private": true,
  "scripts": {
    "test": "node --check app.js && node tests/smoke.js"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
